import { OrdenPublicacionesPipe } from './orden-publicaciones.pipe';

describe('OrdenPublicacionesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenPublicacionesPipe();
    expect(pipe).toBeTruthy();
  });
});
